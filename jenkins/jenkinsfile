pipeline {
    agent any
    
    stages {
        stage('Clone') {
            steps {
                checkout scm
            }
        }
        
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t odoo-pregodi:latest -f Dockerfile .'
            }
        }
        
        stage('Push to Minikube') {
            steps {
                sh 'eval $(minikube docker-env) && docker build -t odoo-pregodi:latest -f Dockerfile .'
            }
        }
        
        stage('Deploy with Ansible') {
            steps {
                sh 'ansible-playbook -v -i ansible/inventory.ini ansible/playbook.yml'
            }
        }
    }
    
    post {
        success {
            echo 'Déploiement réussi!'
        }
        failure {
            echo 'Échec du déploiement!'
        }
    }
}

